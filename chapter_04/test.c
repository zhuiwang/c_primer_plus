#include <stdio.h>
#include <limits.h>

int main(int argc, const char *argv[])
{
    printf("Maximum int value on this system = %d\n", INT_MAX);
    printf("Minmum int value on this system = %d\n", INT_MIN);
    return 0;
}
